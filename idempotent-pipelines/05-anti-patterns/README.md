# Anti-Patterns

The most common ways pipelines break idempotency — each with a concrete fix.

`01_anti_patterns.py` covers 8 anti-patterns, each structured as:
1. The bad pattern (with code)
2. Why it breaks idempotency
3. The fix (with code)
